# Copilot extension approval-scope fixture

Researcher-owned bug-bounty fixture. Each extension writes one empty marker inside its own installed directory and joins the current Copilot session. It performs no shell execution, network access, dependency installation, secret access, persistence outside the disposable extension scope, or destructive operation.
